services:
  postgres:
    image: postgres:17-alpine
    container_name: hankoduko-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-hankoduko}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme_in_production}
      POSTGRES_DB: ${POSTGRES_DB:-hankoduko}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-hankoduko}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
